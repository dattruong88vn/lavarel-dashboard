import { combineReducers } from 'redux';
let initState = {};
export const dealCheckingPlaningReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default combineReducers({dealCheckingPlaningReducer})